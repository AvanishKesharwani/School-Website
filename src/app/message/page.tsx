import MessageClient from "./MessageClient";

export const metadata = {
  title: "Message | Manka Public School",
  description: "Read messages and statements from the desk of our Principal and management.",
};

export default function MessagePage() {
  return <MessageClient />;
}
