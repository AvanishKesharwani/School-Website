import ParentsCornerClient from "./ParentsCornerClient";

export const metadata = {
  title: "Parents' Corner | Manka Public School",
  description: "Important updates, resources, and notices for parents of Manka Public School.",
};

export default function ParentsCornerPage() {
  return <ParentsCornerClient />;
}
